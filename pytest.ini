[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers -p no:cacheprovider
markers =
    ffmpeg: utilise le vrai FFmpeg de l'image (clips générés à la volée)
filterwarnings =
    ignore::DeprecationWarning
    ignore:Using `httpx` with `starlette.testclient`
